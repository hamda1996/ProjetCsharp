@model IEnumerable<FilRougeApi.Models.Formation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duree)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date_Deb)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Personne)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date_Deb)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Personne.PersonneID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FormationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FormationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FormationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
